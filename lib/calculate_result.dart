import 'package:flutter/material.dart';

class calResult extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Calculate"),
      ),
      body: Text("Calculate your BMI"),
    );
  }
}
